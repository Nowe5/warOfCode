cubuklar=[]
while True:
    a = input("sayilar ->")
    if a =='k':
        break
    cubuklar.append(int(a))

def solpivot():
    temp=cubuklar[0]
    for i in range(len(cubuklar)):
           
    if cubuklar[i]<temp:
        cubuklar=cubuklar[i:]
        a=max(cubuklar[1:])
        enbuyuk=cubuklar.index(a)
        for j in range()
    if cubuklar[i]>temp:
        temp=cubuklar[i]
print(doldurma)
